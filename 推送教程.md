# 推送到 GitHub 并自动编译 IPA（Windows 也能操作）

## 第一步：创建 GitHub 仓库
1. 打开 https://github.com/new
2. Repository name 填：`CreamMusic`
3. 选 Public（免费构建额度更多），不要勾选 README
4. 点 Create repository

## 第二步：在 Windows 上推送
安装 Git（https://git-scm.com）后，打开命令行，依次执行：

```bash
# 进入解压后的 CreamMusicIOS 文件夹
cd CreamMusicIOS

# 初始化 git
git init
git add .
git commit -m "奶油音乐 iOS 源码"

# 关联你的仓库（把 你的用户名 换成你的 GitHub 用户名）
git branch -M main
git remote add origin https://github.com/你的用户名/CreamMusic.git
git push -u origin main
```

推送时会要求输入 GitHub 用户名和密码（密码用 Personal Access Token，不是登录密码）。

## 第三步：获取 Personal Access Token
1. 打开 https://github.com/settings/tokens
2. Generate new token → Generate new token (classic)
3. 勾选 `repo` 权限
4. 生成后复制 token，推送时当密码用

## 第四步：下载 IPA
1. 推送后打开你的仓库页面
2. 点上方的 **Actions** 标签
3. 点最新的一次构建（Build IPA）
4. 等它跑完（约 5~10 分钟）
5. 页面底部 **Artifacts** 里下载 `奶油音乐-IPA`
6. 解压后就是 `奶油音乐.ipa`

## 注意
- 免费 GitHub 账号每月有 2000 分钟 macOS 构建额度，够用
- 这个 IPA 是未签名的，要装到 iPhone 需要用你的 Apple 开发者账号签名
- 签名可以用 GitHub Actions 加证书，也可以用 AltStore/Sideloadly 侧载
